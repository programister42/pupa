import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.pupa',
  appName: 'pupa',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
